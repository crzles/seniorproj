# Frontend — Role 4

Owned by Role 4 (Map/Location + Frontend).

Not yet set up — tooling choice (React/Vite, plain JS, etc.) is up to whoever owns this role.
Uses Role 3's shared design system components once available.
